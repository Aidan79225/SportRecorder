<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SportRecorder" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/bg_black</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/text_green</item>
        <item name="android:background">@color/bg_black</item>
    </style>
</resources>